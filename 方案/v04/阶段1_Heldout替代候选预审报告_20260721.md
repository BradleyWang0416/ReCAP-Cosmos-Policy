# Human2Robot v04 阶段 1 Held-out 替代候选预审报告

预审时结论：**VERIFIED_CANDIDATE_NOT_AUTHORIZED**。`push_plate_v1` 与 `push_box_two_v1` 都满足阶段 1 的容量、完整来源 SHA 独立性和 H=8/K=8 原始数据契约；按数据质量排序推荐 `push_plate_v1`。该正式 receipt 本身只验证替代候选，不授权修改冻结协议。用户随后已于 2026-07-21 明确批准推荐方案，见第 5 节。

## 1. 正式审计范围

审计在 `cosmos-policy:latest` 固定镜像、物理 GPU `0,2,5,6`、`--network=none` 和完整 v04 preflight 下运行。CUDA、四卡 NCCL、四个本地权重 SHA、编译扩展、离线变量、存储和 v03 freeze 复核全部通过。

- 冻结基线：16 个 seen 与原 4 个 held-out，共 20 个任务、1,046 个 HDF5；
- 替代候选：`push_plate_v1` 51 个、`push_box_two_v1` 50 个；
- 总扫描：1,147 个 HDF5；
- 原始清单摘要 SHA256：`3d1008bc879604a3168132317e2560e55fce3cfd0d65dc24ded923e9cd004bf9`；
- 两个候选之间的 source SHA 重叠数：0。

正式运行目录：`/DATA1/wxs/ReCAP_M5B_V04_RUNS/orchestrator_logs/stage1_candidate_assessment_20260721/`。

| attempt | 结果 | receipt SHA256 | status SHA256 |
|---|---|---|---|
| 0001 | PASSED | `1e7e30b948b3c8c3748f07f10ddd86560c09f2cef12385bbe06dec5ffed3807f` | `dda4dce57d4e0118957034615a1e87b5100ac101d2922f8c9b02c6c1c8e4a446` |
| 0002 | PASSED | `6e6072ff085bc831010d9adb7a0a13edd143e6beed873e1d355acdbf7892d20d` | `87c3e78e033c92ceb1ac4ae1fa02a3fa27ab13ba89c31d121a09b17893067cbc` |

attempt 0002 加强了 attempt 0001 的重叠口径：除合法可分配文件外，也把结构契约拒绝文件纳入冻结基线 SHA 对比。两候选的“全部原始文件重叠数”和“合法文件重叠数”都为 0；下文以 attempt 0002 为最终证据。

## 2. 候选对比

| 候选 | 原始文件 | 合法文件/唯一 SHA | 与冻结基线重叠 | v03 历史来源 | 合法窗口总数 | 帧数范围 | 拒绝 | 分配 35 后 reserve |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| `push_plate_v1` | 51 | 51/51 | 0 | 0 | 22,795 | 373–593 | 0 | 16 |
| `push_box_two_v1` | 50 | 49/49 | 0 | 0 | 8,459 | 130–283 | 1 | 14 |

`push_box_two_v1/episode_5.hdf5` 少于 H+K=16 帧，已按契约拒绝；剩余 49 个唯一合法来源仍多于 35。两个候选均无 candidate 内重复 SHA。

## 3. 推荐与科学影响

推荐把 `grab_pencil1_v1` 替换为 `push_plate_v1`，理由限于已验证事实：51/51 文件全部合法、来源零重叠、可用 episode 与 reserve 更多，并避免新增第二个 push-box 任务。后一点是任务多样性判断，不是由 SHA 审计直接证明的性能结论。

候选任务在 v03 canonical 与 P1 pool 清单中的历史来源都是 0，不能把任意新 episode 伪装成 legacy quarantine。若批准推荐方案，协议应同步修订为：

- held-out 列表使用 `grab_cube2_v1`、`push_plate_v1`、`grab_to_plate1_v1`、`push_box_random_v1`；
- legacy quarantine 严格取 v03 两份清单的来源并集，四任务计数分别为 10、0、10、10，总数 30；
- `push_plate_v1` 的 51 个全为新来源，固定 SHA 排序后依次分配 human pool 10、robot dev 5、robot final 20、reserve 16；
- human pool 仍为 40、robot dev 仍为 20、robot final 仍为 80；
- 四个 held-out 原始候选总数从 309 改为 258；在现有合法性结果下 reserve 总数为 88；
- 所有 partition 继续要求 source SHA 两两零重叠，原始数据只读，字段物理隔离规则不变。

## 4. 当前门禁

正式 receipt 明确记录：`protocol_change_authorized=false`、`stage1_prepare_allowed=false`、`stage1_status=BLOCKED_PROTOCOL`。只有用户批准任务替换和上述 quarantine 语义修订后，才能更新总计划与代码常量、重算协议 SHA，并重新执行 `prepare-data --execute` 与 `audit-data --execute`。

## 5. 后续授权

2026-07-21，用户明确回复“批准推荐方案”。该授权允许：

- 用 `push_plate_v1` 替换 `grab_pencil1_v1`；
- 将 `push_plate_v1` 的 legacy quarantine 按真实 v03 历史来源设为 0；
- 更新总计划为 v04.1、升级 stage-1 数据 schema、重算协议 SHA，并执行正式 prepare/audit。

授权不改变候选预审 receipt 的历史字段；阶段 1 是否通过仍必须由新协议下的正式 manifest、lock、物理投影和 audit receipt 证明。
